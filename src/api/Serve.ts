import { TBdUsers } from "../types/reactComponents/basic";

export class Serve{
    public static saveUsers(users: TBdUsers) {
        users
    }
    public static load() {
        return [] as TBdUsers
    }
}
